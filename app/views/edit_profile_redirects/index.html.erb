<h1>Listing edit_profile_redirects</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @edit_profile_redirects.each do |edit_profile_redirect| %>
      <tr>
        <td><%= link_to 'Show', edit_profile_redirect %></td>
        <td><%= link_to 'Edit', edit_edit_profile_redirect_path(edit_profile_redirect) %></td>
        <td><%= link_to 'Destroy', edit_profile_redirect, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Edit profile redirect', new_edit_profile_redirect_path %>
